1. Filter (search) on "Assign Driver"
2. Add modal from button at "Assign Driver"
3. Select gate from the Stages table instead of navigating into Events
9. Edit timestamp with date-time picker instead of Epoch MS. Optionally split date and time in the GUI.
10. Manage driver from the assign menu
11. Selected Rally: Beta, boring Card. Selected should just be a dropdown (select), maybe with search. Remove the table with Select, Edit, Delete.
12. Assign gate from "Open Start page". Make it impossible to start a stage without a gate assigned.
